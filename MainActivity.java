package com.example.blackjackassistant; import android.os.Bundle; import androidx.appcompat.app.AppCompatActivity; public class MainActivity extends AppCompatActivity { @Override protected void onCreate(Bundle savedInstanceState) { super.onCreate(savedInstanceState); setContentView(R.layout.activity_main); // Init core modules CardDetector cardDetector = new CardDetector(); CountManager countManager = new CountManager(); AdviceEngine adviceEngine = new AdviceEngine(); VoiceOutput voiceOutput = new VoiceOutput(this); } }
