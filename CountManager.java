package com.example.blackjackassistant; public class CountManager { private int runningCount = 0; private int decksRemaining = 6; public void updateCount(String card) { // TODO: implement Hi-Lo system // Example: 2–6 = +1, 10–A = -1 } public int getRunningCount() { return runningCount; } public double getTrueCount() { if (decksRemaining == 0) return runningCount; return (double) runningCount / decksRemaining; } }
